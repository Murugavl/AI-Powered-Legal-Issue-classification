package com.legal.document.service;

import org.springframework.stereotype.Service;

/**
 * Legacy PDF generator — replaced by BilingualPdfService.
 * Kept as an empty stub so existing Spring context wiring compiles cleanly.
 */
@Service
public class PdfGeneratorService {
    // No-op. All PDF generation is now handled by BilingualPdfService.
}
